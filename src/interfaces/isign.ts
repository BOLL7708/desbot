// Config
interface ISignConfig {
    enabled: boolean
    width: number
    height: number
    transitionDuration: number
    // Direction of apperance
}

// Settings
interface ISignShowConfig {
    title: string
    image: string
    subtitle: string
    duration: number
}